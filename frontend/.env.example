# Production-OS Frontend Environment Variables
#
# For local development: these are optional (Vite proxy handles /api → localhost:3001)
# For production (Vercel): set VITE_API_URL to the deployed backend
#
# VITE_API_URL=https://your-backend.railway.app/api
# VITE_API_URL=https://your-backend.fly.dev/api
