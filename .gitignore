node_modules/
dist/
.env
.env.local
.env.production
.DS_Store

# Allow .env.example (documentation)
!.env.example

# V7.5 generated reports (regenerated by the hardening scripts)
backend/runtime-integrity-report.json
backend/performance-baseline.json

# V8: raw real factory Excel drop zone — never commit real factory data.
# Only sanitized fixtures (committed elsewhere under backend/fixtures/) are kept.
backend/fixtures/real-samples/*
!backend/fixtures/real-samples/README.md
